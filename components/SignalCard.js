// components/SignalCard.js
// Placeholder dashboard UI component.
//
// This will eventually render a single imbalance signal (e.g. instrument
// name, long/short skew, confidence, last-updated time) as a card on the
// dashboard. More components (charts, source-status indicators, etc.) will
// be added to this folder alongside it.
//
// Not implemented yet — placeholder only.

export default function SignalCard({ signal }) {
  return (
    <div className="rounded-lg border border-gray-200 p-4 text-sm text-gray-500">
      SignalCard placeholder — real rendering not implemented yet.
    </div>
  );
}
